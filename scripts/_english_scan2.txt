files_with_english 37
 16 lib/data/datasources/mock/mock_seed_data.dart
    - Visit Information
    - General Health Assessment of Trees
    - Excellent
    - Good
    - Fair
    - Poor
    - Observed Pest or Disease Presence
    - No major issues
    - Aphids
    - Farm Infrastructure Condition
    - Farmer adoption of agronomic practices
    - Does the farmer require immediate assistance?
 12 lib/shared/widgets/admin_farm_map.dart
    - Farm Network ΓÇö India
    - ${_scale.toStringAsFixed(1)}├ù
    - Pinch to zoom ┬╖ drag to pan ┬╖ tap a pin for farm details
    - $state ($count)
    - $state ΓÇö ${farms.length} farm(s)
    - View Farm Details
    - India farm map
    - Pinch with two fingers ┬╖ drag to pan ┬╖ ${_plotted.length} farms ┬╖ ${_scale.toStringAsFixed
    - States
    - Full screen map
    - Reset view
    - Reset
  6 lib/shared/utils/visit_report_pdf.dart
    - $key: $value
    - Shine Gold ΓÇö Visit Report
    - No written notes.
    - Form Answers
    - Form: ${template.name}
    - Generated by Shine Gold ┬╖ ${DateFormat(
  6 lib/shared/widgets/dashboard_overview.dart
    - YOUR COVERAGE
    - acres
    - Field activity
    - Farm
    - Assigned farms
    - Harvest soon
  6 lib/features/super_admin/nearby/admin_nearby_farms_section.dart
    - Within ${AdminNearbyConfig.radiusKm.toStringAsFixed(0)} km ┬╖ updates every 3 min
    - View all ${nearby.farms.length} nearby farms
    - Lat ${location.position!.latitude.toStringAsFixed(4)}, 
    - Updated ${timeFormat.format(lastRefresh!.toLocal())}
    - ${farm.distanceKm!.toStringAsFixed(1)} km
    - Within ${AdminNearbyConfig.radiusKm.toStringAsFixed(0)} km radius
  6 lib/features/executive/home/home_screen.dart
    - $_onboardedCount ┬╖ ${_formatAcres(_onboardedAcres)} ac
    - +${_onboardedCount - 8} more onboarded farms
    - ${_todayFarms.length}
    - Harvest ${DateFormat(
    - ONBOARDED
    - PRIORITY
  5 lib/shared/widgets/admin_ui.dart
    - $crop ┬╖ $harvestType
    - Contact & Identity
    - Verified Administrator
    - Full access to farms, team & harvest data
    - ${widget.visitCount} visits
  5 lib/features/executive/checkin/checkin_screen.dart
    - Could not cancel visit: ${formatApiError(e)}
    - Could not start recording: ${formatApiError(e)}
    - Could not stop recording: ${formatApiError(e)}
    - Voice upload failed: ${formatApiError(e)}
    - Photos
  4 lib/shared/services/visit_report_pdf_service.dart
    - Shine Gold ┬╖ Visit Report
    - Page ${context.pageNumber} of ${context.pagesCount}
    - Photo files could not be embedded. Count: ${visit.photos.length}.
    - ${e.key}: ${e.value}
  4 lib/shared/widgets/admin_network_hero.dart
    - Network Overview
    - Across all field operations
    - $totalVisits
    - Total field visits logged
  4 lib/features/super_admin/executives/admin_executive_profile_screen.dart
    - ${_assignedFarms.length} farms
    - ${_visits.length} records
    - ${visit.durationMinutes} min on site
    - Field Executive ┬╖ ${_executive.employeeId}
  4 lib/features/executive/interactions/interaction_form_screen.dart
    - ${options[index]} month${options[index] == 1 ? 
    - ${options.first} mo
    - ${options.last} mo
    - ${options[index]} mo
  4 lib/features/executive/onboard_farm/onboard_farm_screen.dart
    - Onboard Farm
    - ${_harvestDate.day}/${_harvestDate.month}/${_harvestDate.year}
    - ${_farmPhotos.length}/$_maxFarmPhotos
    - Farmer Name
  3 lib/shared/widgets/state_diagnostics.dart
    - Report copied
    - ΓåÆ ${item.hint!}
    - Copy report
  3 lib/shared/widgets/ux_components.dart
    - ${_formatClock(widget.elapsed)} / ${_formatClock(widget.maxDuration)}
    - Tap to record (max ${_formatClock(widget.maxDuration)})
    - ${_index + 1} / ${widget.urls.length}
  3 lib/features/super_admin/password_reset/admin_password_reset_screen.dart
    - Allow ${request.userName} (${request.employeeId}) to set a new 
    - Approved ΓÇö ${request.employeeId} can now set a new password
    - Requested ${dateFormat.format(request.requestedAt.toLocal())}
  3 lib/features/executive/farms/farm_invitations_screen.dart
    - ${invitation.name} added to your farms
    - Farmer: ${invitation.farmerName}
    - ${invitation.distanceKm!.toStringAsFixed(1)} km away
  3 lib/features/executive/farm_detail/farm_detail_screen.dart
    - Aadhar: ${farm.farmer.aadharNumber}
    - ${dateFormat.format(change.oldDate)} ΓåÆ ${dateFormat.format(change.newDate)}
    - By ${change.changedByName} ┬╖ ${DateFormat(
  3 lib/features/executive/profile/executive_profile_screen.dart
    - ${user.homeLat!.toStringAsFixed(5)}, ${user.homeLng!.toStringAsFixed(5)}
    - WeΓÇÖll use your phone GPS and fill the address automatically.
    - Pin ┬╖ ${_lat!.toStringAsFixed(5)}, ${_lng!.toStringAsFixed(5)}
  2 lib/shared/utils/contact_launcher.dart
    - Could not open phone dialer
    - Could not open WhatsApp
  2 lib/shared/utils/whatsapp_launcher.dart
    - Invalid phone number for WhatsApp
    - Could not open WhatsApp
  2 lib/shared/widgets/farm_boundary_map_view.dart
    - India only
    - Recenter on me
  2 lib/shared/widgets/profile_photo_editor.dart
    - Profile photo updated
    - Tap to update photo
  2 lib/features/welcome/welcome_screen.dart
    - SHINE GOLD
    - ORGANIC AGRO INVENTION
  2 lib/features/super_admin/farms/admin_farm_assign_sheet.dart
    - Assign Executives
    - Save Assignment
  2 lib/features/super_admin/profile/admin_profile_screen.dart
    - Change Password
    - Super Admin ┬╖ ${user.employeeId}
  2 lib/features/executive/interactions/interactions_list_screen.dart
    - ${item.landLocation} ┬╖ ${item.acres} ac ┬╖ ${item.currentCrop}
    - Planning ${item.plannedMonths} month${item.plannedMonths == 1 ? 
  1 lib/shared/widgets/address_autocomplete_field.dart
    - Search address
  1 lib/shared/widgets/farm_card.dart
    - ${farm.distanceKm!.toStringAsFixed(1)} km
  1 lib/shared/widgets/farm_map_preview.dart
    - Open in Google Maps
  1 lib/shared/widgets/visit_log_tile.dart
    - View full report
  1 lib/features/auth/forgot_password_screen.dart
    - ${l10n.employeeIdLabel}: $_employeeId
  1 lib/features/visits/presentation/visit_report_screen.dart
    - Photos (${_visit!.photos.length})
  1 lib/features/visits/presentation/widgets/dynamic_visit_form.dart
    - Enter your response
  1 lib/features/visits/presentation/widgets/visit_form_prefill_card.dart
    - Visit Information
  1 lib/features/super_admin/executives/executives_screen.dart
    - Verified ┬╖ ${draft.lat!.toStringAsFixed(5)}, ${draft.lng!.toStringAsFixed(5)}
  1 lib/features/super_admin/harvests/harvests_screen.dart
    - Harvests on ${selected.day}/${selected.month}/${selected.year}
